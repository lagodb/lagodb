[package]
name = "pg-lakebase-storage"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
tokio = { version = "1.0", features = ["full"] }
pgrx = { workspace = true }
object_store = { version = "0.13.1", features = ["aws", "azure", "gcp"] }
serde = { version = "1.0", features = ["derive"] }
bytes = "1.0"
tracing = "0.1"
anyhow = "1.0"

[build-dependencies]
# Removed tonic-build as per corrected architecture
